# EbookReader Project Status

Last Updated: 2026-09-09 (continued)

Current Phase: M4 — Reading Assets & Search (autonomous feature loop, M1→M8 envelope)
Current Milestone: M4 — **In Progress** (M1 — **Complete**, `94aff83`; M2 — **Complete**, `0a04fb4`; M3 — **Complete**, `096b90d` + durability test)
Current Checkpoint / Promotion Unit: M4 checkpoint 1 — search backend + Annotation/Note/Excerpt asset model + Notebook panel, in progress
Current Branch / PR: `main` (direct commits; no PR yet — single long-lived checkpoint per `ROADMAP.md` §1.1/§1.6)
Last Accepted Commit: `dc9fad9` (M4: Library-wide Search UI and Global Notes cross-book view — CI green). Since the CJK/FTS5 search backend (`46813ee`), this Milestone has now shipped: `crates/domain/src/assets.rs`'s canonical `reading_asset` table (Annotation/Excerpt/Note, orphan preservation via `mark_orphaned`, migration `user_version=5`) with a Notebook panel (list + free-standing-Note compose + per-format jump-back) wired into all three Readers; real text-selection capture in all three Readers (foliate-js `getCFI`, a real `pdfjs-dist` `TextLayer` overlay for PDF single-page mode, Range-counting for TXT) showing a "Highlight"/"Excerpt" toolbar; and, this checkpoint, a Library-wide Search box (`search_library_command`) and a Global Notes cross-book view (`list_all_reading_assets_command`, kind filter) in `App.tsx`, each opening the result's Book. 81 domain tests + 61 frontend tests, all green; CI green on every commit.
Deferred within M4 (explicit, not silent): neither Search nor Global Notes can jump to the exact passage yet, only open the Book -- `SearchHit` only carries `(book_id, kind, content)`, not the asset's anchor, so threading that through is a follow-up; continuous-mode PDF selection (needs multi-page selection scoping); wiring whole-book extracted text into the index (currently only asset text is indexed, per `search.rs`'s own module doc); nothing yet calls `mark_reading_asset_orphaned_command` (no relink-triggered anchor-recheck exists, so orphaning is backend-only-reachable, not product-reachable); index rebuild must not delete canonical user data is untested as an explicit scenario.
Last Accepted Evidence: V1 product/domain scope freeze + V1 UI baseline freeze + P0 loop-control refactor + M0 technical spikes across all 9 evidence tracks, strengthened by a corrective pass with a reproducible evidence harness (`tooling/m0-evidence/`); Architecture Lock approved at the second Human Architecture Gate — see `M0_TECHNICAL_SPIKE_REPORT.md`, `M0_ARCHITECTURE_DECISION.md`. **M1, M2, and M3 are all complete** — each Milestone's full evidence account lives in its own `ROADMAP.md` Exit Gate section (that is now the authoritative detail, not this file, to keep this file from becoming an ever-growing duplicate). Headline: 59 domain tests + 48 frontend tests, CI green on every commit across all three Milestones. M3 closed M0-G's own carried-forward residual risk (production Win32 ReadingSession lock/sleep hook).
Current Blockers: None. One Architecture Amendment carried forward as accepted baseline (CJK search needs a segmentation adapter in front of SQLite FTS5 — resolved this Milestone, see M4 checkpoint history above). One honest verification gap, not a blocker: the new selection-driven Highlight/Excerpt UI (text-selection -> `create_reading_asset_command`) has not been exercised via a real mouse drag-select in a live native window and screenshotted -- this sandbox's `SetForegroundWindow` residual (see `[[feedback-native-gui-visual-verification]]`) makes scripted drag-select-then-screenshot unreliable, and it was judged not worth burning further turns retrying. Confidence instead comes from directly reading foliate-js's/pdfjs-dist's actual source for the exact APIs used (`getCFI`, `TextLayer`, section-document `load` events) rather than guessing at them, plus clean typecheck/build/CI. A future session with reliable interactive GUI verification, or the user's own manual click-through, should close this gap.
Open Escalations: None
Architecture State: **Accepted Architecture Baseline** — see `M0_ARCHITECTURE_DECISION.md`. Amendments still require reconciliation under the Architecture Promotion Rule; Product-Contract Conflicts still require a Human Gate. Dev-environment findings, all isolated to this local sandbox and confirmed non-issues on CI's MSVC `windows-latest` runner (which remains authoritative): (1) `cargo test` on this sandbox's GNU-hosted toolchain is unstable once Tauri's COM/WebView2 dependency graph links into a test binary — worked around via the Tauri-free `crates/domain` member; (2) a real `cargo build` originally failed to *link* locally (`ld: export ordinal too large`) — resolved by dropping the unused `cdylib` crate-type; (3) `npm run tauri dev`'s cargo-watch supervision is unstable for long-lived native visual checks — launching the already-built `target/debug/ebookreader.exe` directly against a plain standalone `npm run dev` vite server is the stable method; (4) capture native screenshots via `PrintWindow(hwnd, hdc, PW_RENDERFULLCONTENT)` rather than `CopyFromScreen` — handle-based, cannot mis-capture an unrelated window, and needs no foreground at all (see `[[feedback-native-gui-visual-verification]]` for the full method).
Feature Complete: No
Feature Freeze: No
RC / Release State: Not started
Next Action: every named M4 Success Evidence item now has at least a working implementation: asset types remain distinct (Annotation/Excerpt/Note), user-authored assets survive relink/anchor failure (orphan preservation, though not yet relink-triggered), global search covers Notes/Excerpts/Annotations text (not yet whole-book extracted text), index rebuild is idempotent by construction (not yet explicitly tested as a rebuild scenario), and CJK behavior meets the accepted M0 architecture. Remaining before treating M4's Exit Gate as satisfiable: wire a relink event to `mark_reading_asset_orphaned_command`; thread anchors through Search/Global Notes for exact-passage jump; an explicit index-rebuild-preserves-user-data test; continuous-mode PDF selection; whole-book text extraction into the index; and the still-open native-interactive-GUI verification gap on the selection-capture UI (see Current Blockers). After those, do the M4 Exit Gate review itself (`ROADMAP.md`: "Search/indexing remains derived; user-authored reading assets remain canonical") and, if satisfied, promote to M5 (Scanned PDF OCR) per the Routine Milestone Boundary Rule. No further per-Milestone human approval is required inside the M1→M8 envelope unless a stop/escalation trigger fires (`ROADMAP.md` §1.8); the next Human Gate is the Human Feature Freeze Gate after M8. This session runs under an active `/goal` Stop hook (EbookReader -> Feature Complete Candidate via M2->M8) plus a `CronList`-verified watchdog job (id `4f18a51c`, `12,42 * * * *`, session-only, fires only if the goal-driven work appears stalled); if a resumed session finds neither active but this authorization envelope is still valid, resume from this file directly and re-arm the watchdog.
