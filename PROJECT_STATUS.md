# EbookReader Project Status

Last Updated: 2026-09-09

Current Phase: M5 — Scanned PDF OCR (autonomous feature loop, M1→M8 envelope)
Current Milestone: M5 — **In Progress** (M1 — **Complete**, `94aff83`; M2 — **Complete**, `0a04fb4`; M3 — **Complete**, `096b90d` + durability test; M4 — **Complete**, `7f12173`, full Exit Gate evidence in `ROADMAP.md`)
Current Checkpoint / Promotion Unit: M5 checkpoint 6 — every `ROADMAP.md` M5 Success Evidence bullet has now been addressed (most closed for real, two -- CJK/multi-column quality and full pause/resume/cancel granularity -- closed as honestly-scoped, evidenced residuals rather than silently ignored). M5 is close to Exit-Gate-ready; see Next Action for what's still genuinely open.
Current Branch / PR: `main` (direct commits; no PR yet — single long-lived checkpoint per `ROADMAP.md` §1.1/§1.6)
Last Accepted Commit: `f53efd3`. History: checkpoint 1 (`1a9a367`) shipped the scanned-PDF degraded-state notice (SS13.1/13.2). Checkpoint 2 (`4ab698e`) added the OCR job/correction data model with a domain test directly proving the Exit Gate condition (a correction survives a raw-cache clear). Checkpoint 3 resolved model sourcing per explicit user direction (reuse of already-license-cleared PP-OCRv6 assets already present on this machine; hashes/provenance in `tooling/m5-evidence/README.md`). Checkpoint 4 (`55130a6`) root-caused the detector's degenerate output to a double-sigmoid bug and shipped a working postprocessing pipeline after replacing two panicking `imageproc::geometry` calls with a hand-rolled hull/rect implementation (142/142 real boxes recovered, matching the Python reference exactly). Checkpoint 5 (`16eed51`) ported detection+postprocessing to production, closed out recognition (`PP-OCRv6_rec_small.onnx`, embedded 18,708-char dictionary, also already-softmax) and the orientation classifier (`ch_ppocr_mobile_v2.0_cls_mobile.onnx`, also already-softmax) into `OcrEngine::process_page()`, wired a real Tauri command (`run_ocr_job_command`) and a "Run OCR" button in `PdfReader.tsx`. 121 domain tests (was 91 before M5 detector work resumed), 1 local-only integration test, 63 frontend tests.

**This checkpoint** closed the remaining gaps against `ROADMAP.md`'s explicit M5 Success Evidence list: (1) full **Current Page / Selected Pages / Entire Book** scope selection in the UI (page-range text input, e.g. "1,3,5-7"), not just Current Page; (2) real **pause/resume/cancel**: `run_ocr_job_command` checks the job's stored status before each page and stops early if a concurrent `set_ocr_job_status_command` call (fired by new Pause/Cancel buttons while the run is in flight) set it to Paused/Cancelled; Resume re-derives which originally-requested pages still lack a saved result and re-sends only those -- real, not just the data-model shape for it; (3) OCR text now feeds **Library-wide Search**: `run_ocr_job_command` indexes each page's raw result (`search::index_text`, kind `book_text`), and `save_ocr_correction_command` re-indexes with the corrected text, closing `PRODUCT_SPEC.md` SS12's explicitly-named "corrected OCR text" source, the one source M4 could not close because M5 didn't exist yet; (4) **Excerpt/Annotation capture now works on OCR'd text**: the recognized-text paragraph is plain selectable DOM text, so a second ref alongside the pdf.js TextLayer ref in the existing selection listener was enough -- the anchor/jump-back mechanism needed no changes, it was already page-number-based; (5) **representative CJK/multi-column quality**: ran the real pipeline against `ia_200.jpg` (the exact real, public-domain, degraded fixture M0's corrective pass used) and replaced the earlier qualitative M0 finding with concrete evidence -- single-column English recognizes coherently (modulo the already-known word-spacing loss), but the two-column commentary demonstrably splices and vertical CJK columns are recognized character-for-character but ordered wrong (left-to-right instead of right-to-left). Deliberately did **not** ship a speculative column-clustering fix without strong validation -- a naive x-gap heuristic risks silent regressions on headers/footers/indented paragraphs, and shipping unvalidated "fixes" would be worse than an honestly-documented residual; (6) verified **"original PDF untouched"** by construction -- grepped `src-tauri/src` and `crates/domain/src` for any write path touching a source book file; none exists, the pipeline only ever reads bytes to render and writes to the app's own SQLite DB. Full findings: `tooling/m5-evidence/m5e_multi_column_cjk_evidence.md`.
Current Blockers: None. Three honest verification gaps, not blockers: (1) carried from M4, the selection-driven Highlight/Excerpt UI has not been exercised via a real mouse drag-select in a live native window and screenshotted -- this sandbox's `SetForegroundWindow` residual (see `[[feedback-native-gui-visual-verification]]`) makes scripted drag-select-then-screenshot unreliable; (2) the "Run OCR" button, scope selection, pause/resume/cancel, and correction-editor flow have not been click-tested in a live native window either, for the same reason -- Tauri's `invoke()` IPC bridge only exists inside the native WebView, so a plain browser preview cannot exercise any of it, not even partially; (3) the uncapped recognition-width change (`m5d_ocr_engine_production_port.md`) has not been re-measured against the M5-C fixture's specific boxes to confirm it actually improves accuracy versus just being a principled correction. Confidence for (1)/(2) comes from source-level correctness review plus clean typecheck/build/CI (63 frontend + 121 domain tests + 2 local-only integration tests). A future session with reliable interactive GUI verification, or the user's own manual click-through, should close (1)/(2).
Open Escalations: None
Architecture State: **Accepted Architecture Baseline** — see `M0_ARCHITECTURE_DECISION.md`. Amendments still require reconciliation under the Architecture Promotion Rule; Product-Contract Conflicts still require a Human Gate. M0's accepted OCR backend decision (§8): a PP-OCR-class ONNX pipeline via the Rust `ort` crate (dynamically-loaded `onnxruntime.dll`, MSVC target for release packaging — `ort-sys` ships no prebuilt `x86_64-pc-windows-gnu` binary). M0's carried-forward OCR residuals for M5 to own: CJK/Latin word-boundary spacing loss in OCR output; multi-column text splicing across column boundaries; incorrect reading order on vertical classical-Chinese layouts (all confirmed on real pages during M0's corrective pass, not hypothetical) — M5's correction-mapping and layout-analysis design must plan for these. Dev-environment findings, all isolated to this local sandbox and confirmed non-issues on CI's MSVC `windows-latest` runner (which remains authoritative): (1) `cargo test` on this sandbox's GNU-hosted toolchain is unstable once Tauri's COM/WebView2 dependency graph links into a test binary — worked around via the Tauri-free `crates/domain` member; (2) a real `cargo build` originally failed to *link* locally (`ld: export ordinal too large`) — resolved by dropping the unused `cdylib` crate-type; (3) `npm run tauri dev`'s cargo-watch supervision is unstable for long-lived native visual checks — launching the already-built `target/debug/ebookreader.exe` directly against a plain standalone `npm run dev` vite server is the stable method; (4) capture native screenshots via `PrintWindow(hwnd, hdc, PW_RENDERFULLCONTENT)` rather than `CopyFromScreen` — handle-based, cannot mis-capture an unrelated window, and needs no foreground at all (see `[[feedback-native-gui-visual-verification]]` for the full method).
Feature Complete: No
Feature Freeze: No
RC / Release State: Not started
Next Action: Continue M5 autonomously (no further per-checkpoint approval required unless a genuine ROADMAP Stop/Escalation condition fires). Every M5 Success Evidence bullet has now been addressed; what's left before M5 can be marked Complete: (1) the Exit Gate itself ("manual corrections survive raw OCR/cache rebuild and restart") -- the data-model half has been proven since checkpoint 2 (`a_correction_survives_clearing_the_raw_ocr_cache`), and this session's checkpoints proved the real UI writes through that same real `save_page_result`/`save_correction` path, but a full click-through demonstration (run OCR, correct a page, clear cache, confirm the correction persists, restart the app, confirm it's still there) has not been done -- the native-GUI verification gap (see Current Blockers) is what's actually blocking this, not missing functionality; (2) a real layout-analysis pass for the CJK/multi-column reading-order residual, or an explicit, justified decision that it stays deferred past M5 -- `tooling/m5-evidence/m5e_multi_column_cjk_evidence.md` has the concrete evidence and defect pattern for whoever picks this up; (3) re-measure the uncapped-recognition-width change for actual accuracy impact, not just principled correctness. None of these are new Milestone scope -- they're finishing M5's own already-identified Success Evidence and Exit Gate, so this session should keep working on them (or hand off with this status accurate) rather than jump to M6. The `/goal` Stop hook (EbookReader -> Feature Complete Candidate via M2->M8) remains active for this session.
