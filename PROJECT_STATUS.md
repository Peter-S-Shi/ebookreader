# EbookReader Project Status

Last Updated: 2026-09-10 (Human Acceptance Corrective Batch #3)

Current Phase: **Feature Complete Corrective Pass — complete; second Feature Complete Candidate produced, awaiting Human Feature Freeze Gate.** The first Human Feature Freeze Gate, on the `f47452f` Feature Complete Candidate, was **NOT APPROVED** — several frozen V1 behaviors were backend-only, prototype-only, or unwired despite Milestone-complete/CI-green status. The corrective pass (read-only workspace audit, a real requirement-to-implementation coverage audit against production source, and an ordered 20-ticket list) is now finished: all 20 tickets are CLOSED and `FEATURE_COMPLETE_REQUIREMENT_COVERAGE_AUDIT.md` shows zero open gaps. `FEATURE_COMPLETE_CANDIDATE_REPORT.md` was rewritten as the second candidate (commit `ebe33f6`). M1-M8 themselves remain Complete (see below); what changed across the whole corrective pass is that "Milestone Complete" is no longer being treated as proof of user-reachable completeness.
Current Milestone: M1 — **Complete**, `94aff83`; M2 — **Complete**, `0a04fb4`; M3 — **Complete**, `096b90d` + durability test; M4 — **Complete**, `7f12173`; M5 — **Complete**, `67d32c5`; M6 — **Complete**, `9e99099`; M7 — **Complete**, `4d86045`; M8 — **Complete**, `2b81d54`. Full Exit Gate evidence for every Milestone is in `ROADMAP.md`. This status is unaffected by the corrective pass — the M1-M8 envelope closed correctly; the gap was in what the Feature Complete Candidate promotion inferred from it.
Current Checkpoint / Promotion Unit: **Feature Complete Corrective Pass**, ticket-by-ticket per `FEATURE_COMPLETE_CORRECTIVE_TICKETS.md`. Tickets 1 and 2 are CLOSED on `main` as of `a70a8f6` with GitHub CI green; Ticket 3 (FC-C04 destructive semantics separation) is CLOSED on `22afafc` with CI run 34402533583 green; Ticket 4 (FC-C03 Book Data completed-read override UI) is CLOSED on `2de37bc` with CI run 34403600204 green; Ticket 5 (FC-A03 duplicate-fingerprint dialog) is CLOSED on `bb3ac72` with CI run 34410909121 green; Ticket 6 (FC-A01 Collections and Tags) is CLOSED on `c54ea86` with CI run 34412338912 green; Ticket 7 (FC-A02 metadata editing UI + user-correction precedence) is CLOSED on `df4e570` with CI run 34413175475 green; Ticket 8 (FC-A04 Notebook Markdown export) is CLOSED on `dc4eb4d` with CI run 34414007149 green; Ticket 9 (FC-C07 Reference-file picker for Full Library Backup) is CLOSED on `c3b02eb` with CI run 34414736330 green; Ticket 10 (FC-C08 non-blocking startup update check + preference) is CLOSED on `6a65420` with CI run 34415666414 green; Ticket 11 (FC-A05 Book Hours configuration UI + revision history) is CLOSED on `135a7ff` with CI run 34416801485 green; Ticket 12 (FC-A06 Actual Reading Time policy controls) is CLOSED on `bed54ba` with CI run 34417941868 green; Ticket 13 (FC-A07 recovery snapshot before schema migration and destructive mutations) is CLOSED on `6281332` with CI run 34419014285 green; Ticket 14 (FC-A08 typography/font provenance) is CLOSED on `0809325` with CI run 34432012895 green; Ticket 15 (FC-A09 persist sound toggle + reduced-motion in-app override) is CLOSED on `a4c0c62` with CI run 34432709301 green; Ticket 16 (FC-A10 Reading Checkpoint) is CLOSED on `ffd761d` with CI run 34433233529 green; Ticket 17 (FC-A11 Book Details/Continue Reading/Book Hours presentation) is CLOSED on `6bf6f56` with CI run 34476328787 green (implementation `2edc641`, followed by a flaky-test fix commit after its first CI run failed); Ticket 18 (FC-A14 Backup/Restore completeness) is CLOSED on `ffc3072` with CI run 34477080424 green. All 18 originally-listed tickets are now CLOSED, and all 8 confirmed defects (FC-C01-FC-C08) from the first Human Feature Freeze Gate remain closed. A post-Ticket-18 reconciliation pass re-auditing FC-C05 against actual production source (not the ticket table's own claim) found it still `IMPLEMENTED_PARTIAL`: a default import mode setting and an About & Updates Settings section were never actually delivered by any of tickets 9-16 despite the original dependency table implying full coverage. Two new tickets were added and both are now CLOSED: Ticket 19 (FC-A15 default import mode setting) on `a9a136f` (CI run 34478296463 green), Ticket 20 (FC-A16 About & Updates Settings section) on `5e3b20b` (CI run 34479082655 green). All 20 tickets are CLOSED and `FEATURE_COMPLETE_REQUIREMENT_COVERAGE_AUDIT.md` shows zero `MISSING`/`IMPLEMENTED_PARTIAL`/`IMPLEMENTED_BACKEND_ONLY`/`PROTOTYPE_ONLY` rows as of this reconciliation. Do not self-promote to Feature Freeze or start M9 Product Hardening -- the second Feature Complete Candidate package (native/manual acceptance report, revised candidate report) is still required before a Human Feature Freeze Gate can be requested.
Current Branch / PR: `main` (direct commits; no PR — single long-lived checkpoint per `ROADMAP.md` §1.1/§1.6)
Last Accepted Commit: `4841fc6` ("Human Acceptance Corrective Batch #3", HA-010 + HA-011; GitHub CI run 34505846824 green). M8's `2b81d54` remains the last Milestone-envelope commit.

**Human Acceptance Corrective Batch #1 (2026-09-10, `1814a40`):** while gathering the real native-desktop human acceptance evidence the second Feature Complete Candidate still lacked (see `Feature Complete` line below), the user's own click-through of the actual Tauri desktop app found 8 real defects -- 3 P0 blockers (EPUB fresh-open blank body, Dark theme not reaching EPUB content, Settings' Typography panel covering the rest of the page), 3 P1 UX (toolbar crowding, unstyled Settings, oversized Reader overlay), 2 P2 UI (stray list markers, Contents header spacing) -- none of which the 20-ticket corrective pass or its CI had caught, since they required an actual rendered UI, not source review or jsdom-based tests. GitHub CI run 34497573797 green, reviewed via `/code-review` with no blocking findings. This batch does not alter `FEATURE_COMPLETE_REQUIREMENT_COVERAGE_AUDIT.md`'s per-requirement classifications and does not touch `PRODUCT_SPEC.md`/`DESIGN.md`.

**Human Acceptance Corrective Batch #2 (2026-09-10, `2fa751b`):** the user's own real desktop retest of Batch #1 confirmed HA-001 through HA-006 and HA-008 hold, but found HA-007's fix incomplete (explicit Light still left the app shell dark on a dark OS; EPUB text was still unreadable) and one new defect, HA-009 (the Reader's Aa Typography overlay had a light panel background with light-colored labels in Dark/Match-System-dark mode -- effectively invisible). Root-caused via CSS specificity analysis to a Vite-template leftover unguarded dark-mode `:root` rule (HA-007 shell), CSS inheritance not reaching descendant elements with their own explicit color (HA-007 EPUB text), and a specificity regression introduced by Batch #1's own HA-008 fix (HA-009). Fixed with a `/tdd`+`/diagnosing-bugs` loop: `src/App.theme.test.ts` (new) parses the real `App.css` source text to assert the bug pattern is gone; `typography.test.ts` gained a descendant-color assertion; both confirmed red before the fix, green after; additionally verified live against the real built CSS in a running Vite dev server across 6 shell theme/OS combinations and 2 panel-contrast combinations via `getComputedStyle`. GitHub CI run 34501368395 green. HA-006's already-closed sizing/hosting contract and HA-001-005/008 were not reopened or altered.

**Human Acceptance Corrective Batch #3 (2026-09-10, `4841fc6`):** the user's own real desktop retest after Batch #2 found two newly-observed defects and did not reopen already human-closed items: HA-010 (Windows Dark + explicit Light correctly reached the app shell and EPUB content, but Settings Typography and Reader Contents remained Dark) and HA-011 (real EPUB Reader in paginated Double Page mode had no working Left/Right arrow or wheel/touchpad page navigation). Root-caused with `/tdd`+`/diagnosing-bugs`: HA-010 was a component-level contextual-surface theme contract problem caused by scattered hard-coded light/dark panel backgrounds and direct `prefers-color-scheme` overrides; HA-011 was missing production Reader input wiring despite `foliate-js` already exposing navigation APIs (`goLeft()`/`goRight()` for publication-direction-aware physical arrow keys, `prev()`/`next()` for reading-order movement). Fixed with shared contextual-surface tokens for Typography, Contents, Notebook, selection toolbar, and completion prompt, plus paginated-only keyboard/wheel Reader navigation that leaves Continuous Scroll natural and lets controls/overlays handle their own input. Red-before-green regression evidence was added in `src/App.theme.test.ts` and `src/Reader.test.tsx`; local frontend verification passed (`npm test -- App.theme.test.ts Reader.test.tsx`, full `npm test`, `npm run typecheck`, `npm run build`), local Tauri build passed (`cargo build --manifest-path src-tauri\Cargo.toml`), and GitHub CI run 34505846824 was green. Local Windows `cargo test -p ebookreader-domain --lib` still exits abnormally with `0xc0000022` before test output, while CI Rust tests pass. No Feature Freeze approval, M9 work, or UI migration work was started.

All three Human Acceptance batches are recorded in `HUMAN_ACCEPTANCE_DEFECT_REGISTER.md`: HA-001-006, HA-008 are `HUMAN-ACCEPTANCE CLOSED` per the user's own reported retest; HA-007, HA-009, HA-010, and HA-011 are `IMPLEMENTED / READY FOR HUMAN RETEST`, explicitly **not** `HUMAN-ACCEPTANCE CLOSED` -- that register's own minimal retest checklist requires the user's own real desktop confirmation before that status can change. These batches do not alter `FEATURE_COMPLETE_REQUIREMENT_COVERAGE_AUDIT.md`'s per-requirement classifications or `PRODUCT_SPEC.md`/`DESIGN.md`.

**M8 summary (full detail in `ROADMAP.md` Milestone 8 section):** `crates/domain/src/backup.rs` implements real Backup/Restore by copying the canonical SQLite database file itself into a zip archive with a manifest -- never a hand-rolled per-table export that could silently diverge from what the app actually persists -- reusing the exact manifest/preview → safety snapshot → restore → verify workflow the M0 corrective-pass spike already validated. `preview()` never mutates anything on disk; `restore()` refuses an incomplete archive outright (proven directly, live database confirmed untouched after rejection); a recovery snapshot of the live database is created before any replacement; "canonical user-asset round trip" is proven with a dedicated test carrying a Note, an OCR correction, and a Book Hours config through a real round trip. `restore_backup_command` safely swaps the live `rusqlite::Connection` around the file-level replacement so it never races a held file handle. `DataRecovery.tsx` provides Create Backup / Preview / destructive-confirmation / Restore, plus Update Awareness (a stable-release check against GitHub Releases' `/releases/latest`, which already excludes drafts/prereleases by GitHub's own semantics). 9 new domain tests (151 total, was 143), 16 new frontend tests (89 total, was 73), CI green.

**M1-M7 summaries:** preserved in full in `ROADMAP.md`'s per-Milestone sections and this file's own git history; not restated here to keep this file current rather than an accumulating log. The single self-correction of note: M5 was first marked Complete on Exit Gate/Success Evidence grounds alone at commit `51e506e`, then found (by this project's own self-review, before any user or CI involvement) to have a real UI-composition gap against `DESIGN.md`'s frozen authority; corrected in the open at commit `67d32c5`, with the original overstatement stated plainly rather than silently amended.

Current Blockers: None. See `FEATURE_COMPLETE_CANDIDATE_REPORT.md` §3 for the full residual-risk list carried forward from M0 and each Milestone. The two residuals recurring across every recent Milestone: (1) this sandbox's `SetForegroundWindow` limitation (`[[feedback-native-gui-visual-verification]]`) has made native-window click-through/screenshot verification unreliable to script for M4 onward's UI work -- confidence instead comes from source-level review plus clean typecheck/build/CI on every commit; (2) vertical classical-Chinese OCR reading order is root-caused (a detection-orientation problem) but not yet fixed, requiring its own future architecture decision (rotation-reprocessing or a dedicated text-direction model) -- carried forward explicitly, not silently dropped, since M5.
Open Escalations: None. No Stop/Escalate condition was triggered in any Milestone M1-M8.
Architecture State: **Accepted Architecture Baseline** — see `M0_ARCHITECTURE_DECISION.md`. Amendments still require reconciliation under the Architecture Promotion Rule; Product-Contract Conflicts still require a Human Gate. All M0-named architecture decisions (OCR backend, DocumentLocation cross-format design, CJK search adapter, Backup/Restore workflow, ReadingSession lock/sleep semantics) have been implemented and evidenced by their owning Milestone; M7 additionally closed `FORMAT_CAPABILITY_MATRIX.md`'s un-spiked `🧪` markers on Bilingual Alignment Package via a recorded Loop Engineering evidence pass (`tooling/m7-evidence/`) rather than by silently building past them. Full M0 residual-risk disposition: `FEATURE_COMPLETE_CANDIDATE_REPORT.md` §3.
Feature Complete: **Second candidate produced, commit `ebe33f6`** — see `FEATURE_COMPLETE_CANDIDATE_REPORT.md`. All 20 corrective-pass tickets CLOSED; coverage audit shows zero `MISSING`/`PARTIAL`/`BACKEND_ONLY`/`PROTOTYPE_ONLY` rows. Native/manual QA click-through against `MANUAL_QA.md`'s full checklist was **not** performed in this sandbox (the same `SetForegroundWindow` limitation carried forward since M4 -- see the candidate report §5); the user's own real-desktop click-through (Human Acceptance Corrective Batches #1-#3, above) is closing exactly that residual: 7 of 11 found defects are now `HUMAN-ACCEPTANCE CLOSED`, 4 (HA-007, HA-009, HA-010, HA-011) are `IMPLEMENTED / READY FOR HUMAN RETEST` pending the user's next retest (`HUMAN_ACCEPTANCE_DEFECT_REGISTER.md`), not yet folded back into a revised candidate report.
Feature Freeze: No — not self-approved, not requested. Explicitly still out of scope: these batches fixed real defects found during human-acceptance evidence gathering, but did not and do not promote Feature Freeze or begin M9.
RC / Release State: Not started.
Next Action: **HARD STOP.** Commit `4841fc6` and its CI evidence are complete. Awaiting the user's own real Tauri desktop retest of HA-007, HA-009, HA-010, and HA-011 (checklist in `HUMAN_ACCEPTANCE_DEFECT_REGISTER.md`) before that register can move those four to `HUMAN-ACCEPTANCE CLOSED`, and awaiting a human review of `FEATURE_COMPLETE_CANDIDATE_REPORT.md` for the separate Human Feature Freeze Gate decision. Do not self-promote into Feature Freeze or start M9 Product Hardening under any circumstance until that gate is explicitly granted.
