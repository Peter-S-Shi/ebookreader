# EbookReader Project Status

Last Updated: 2026-09-09

Current Phase: **Feature Complete Candidate** — M1 through M8 all Complete under the authorized autonomous engineering envelope. Per `ROADMAP.md`'s Feature Complete Candidate Gate, this session performed cross-milestone evidence reconciliation, canonical documentation reconciliation, a residual-risk list, and a Feature Complete Candidate report (`FEATURE_COMPLETE_CANDIDATE_REPORT.md`), then **HARD STOPPED** without self-promoting into Feature Freeze or Product Hardening. Awaiting the Human Feature Freeze Gate.
Current Milestone: M1 — **Complete**, `94aff83`; M2 — **Complete**, `0a04fb4`; M3 — **Complete**, `096b90d` + durability test; M4 — **Complete**, `7f12173`; M5 — **Complete**, `67d32c5`; M6 — **Complete**, `9e99099`; M7 — **Complete**, `4d86045`; M8 — **Complete**, `2b81d54`. Full Exit Gate evidence for every Milestone is in `ROADMAP.md`.
Current Checkpoint / Promotion Unit: None open — the M1→M8 envelope is exhausted. The next checkpoint is the Human Feature Freeze Gate, which only a human can open.
Current Branch / PR: `main` (direct commits; no PR — single long-lived checkpoint per `ROADMAP.md` §1.1/§1.6)
Last Accepted Commit: `2b81d54`.

**M8 summary (full detail in `ROADMAP.md` Milestone 8 section):** `crates/domain/src/backup.rs` implements real Backup/Restore by copying the canonical SQLite database file itself into a zip archive with a manifest -- never a hand-rolled per-table export that could silently diverge from what the app actually persists -- reusing the exact manifest/preview → safety snapshot → restore → verify workflow the M0 corrective-pass spike already validated. `preview()` never mutates anything on disk; `restore()` refuses an incomplete archive outright (proven directly, live database confirmed untouched after rejection); a recovery snapshot of the live database is created before any replacement; "canonical user-asset round trip" is proven with a dedicated test carrying a Note, an OCR correction, and a Book Hours config through a real round trip. `restore_backup_command` safely swaps the live `rusqlite::Connection` around the file-level replacement so it never races a held file handle. `DataRecovery.tsx` provides Create Backup / Preview / destructive-confirmation / Restore, plus Update Awareness (a stable-release check against GitHub Releases' `/releases/latest`, which already excludes drafts/prereleases by GitHub's own semantics). 9 new domain tests (151 total, was 143), 16 new frontend tests (89 total, was 73), CI green.

**M1-M7 summaries:** preserved in full in `ROADMAP.md`'s per-Milestone sections and this file's own git history; not restated here to keep this file current rather than an accumulating log. The single self-correction of note: M5 was first marked Complete on Exit Gate/Success Evidence grounds alone at commit `51e506e`, then found (by this project's own self-review, before any user or CI involvement) to have a real UI-composition gap against `DESIGN.md`'s frozen authority; corrected in the open at commit `67d32c5`, with the original overstatement stated plainly rather than silently amended.

Current Blockers: None. See `FEATURE_COMPLETE_CANDIDATE_REPORT.md` §3 for the full residual-risk list carried forward from M0 and each Milestone. The two residuals recurring across every recent Milestone: (1) this sandbox's `SetForegroundWindow` limitation (`[[feedback-native-gui-visual-verification]]`) has made native-window click-through/screenshot verification unreliable to script for M4 onward's UI work -- confidence instead comes from source-level review plus clean typecheck/build/CI on every commit; (2) vertical classical-Chinese OCR reading order is root-caused (a detection-orientation problem) but not yet fixed, requiring its own future architecture decision (rotation-reprocessing or a dedicated text-direction model) -- carried forward explicitly, not silently dropped, since M5.
Open Escalations: None. No Stop/Escalate condition was triggered in any Milestone M1-M8.
Architecture State: **Accepted Architecture Baseline** — see `M0_ARCHITECTURE_DECISION.md`. Amendments still require reconciliation under the Architecture Promotion Rule; Product-Contract Conflicts still require a Human Gate. All M0-named architecture decisions (OCR backend, DocumentLocation cross-format design, CJK search adapter, Backup/Restore workflow, ReadingSession lock/sleep semantics) have been implemented and evidenced by their owning Milestone; M7 additionally closed `FORMAT_CAPABILITY_MATRIX.md`'s un-spiked `🧪` markers on Bilingual Alignment Package via a recorded Loop Engineering evidence pass (`tooling/m7-evidence/`) rather than by silently building past them. Full M0 residual-risk disposition: `FEATURE_COMPLETE_CANDIDATE_REPORT.md` §3.
Feature Complete: **Candidate** (not yet human-approved — see `FEATURE_COMPLETE_CANDIDATE_REPORT.md`).
Feature Freeze: No — requires explicit human approval at the Human Feature Freeze Gate; this session does not and must not self-promote into it.
RC / Release State: Not started.
Next Action: **None for this autonomous session.** Per `ROADMAP.md`'s Feature Complete Candidate Gate, the agent must not self-promote into Feature Freeze or Product Hardening after producing the Feature Complete Candidate report. The next action belongs to the human: review `FEATURE_COMPLETE_CANDIDATE_REPORT.md` (cumulative evidence, residual risk, canonical docs, authority consistency, unresolved escalations) and either grant explicit approval to transition to Feature Freeze, or send specific corrective instructions back into a new autonomous pass. This session's `/goal` envelope (M2→M8 → Feature Complete Candidate) has reached its own completion condition; the session stops and returns control here.
