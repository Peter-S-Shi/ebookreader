# EbookReader Project Status

Last Updated: 2026-09-09

Current Phase: M8 — Data Safety, Restore & Update Awareness (autonomous feature loop, M1→M8 envelope, final Milestone before Feature Complete Candidate)
Current Milestone: M8 — **Starting** (M1 — **Complete**, `94aff83`; M2 — **Complete**, `0a04fb4`; M3 — **Complete**, `096b90d` + durability test; M4 — **Complete**, `7f12173`; M5 — **Complete**, `67d32c5`, full Exit Gate evidence in `ROADMAP.md`; M6 — **Complete**, `9e99099`, full Exit Gate evidence in `ROADMAP.md`; M7 — **Complete**, full Exit Gate evidence in `ROADMAP.md`)
Current Checkpoint / Promotion Unit: M8 not yet started -- this is the transition point immediately after M7's promotion, and the final Milestone in the M1→M8 autonomous envelope.
Current Branch / PR: `main` (direct commits; no PR yet — single long-lived checkpoint per `ROADMAP.md` §1.1/§1.6)
Last Accepted Commit: pending M7 commit (see git log for the exact hash once pushed).

**M7 summary (full detail, including the Loop Engineering scope decision, in `ROADMAP.md` Milestone 7 section and `tooling/m7-evidence/m7a_bilingual_alignment_scope_decision.md`):** before implementing past `FORMAT_CAPABILITY_MATRIX.md`'s `🧪` markers on Bilingual Alignment Package (never covered by any M0 spike), a real evidence pass found that `PRODUCT_SPEC.md` SS14 already excludes alignment authoring, so "supports Bilingual Alignment Package" only requires extracting and displaying each format's real text in an independent pane -- already-tested extraction paths reused from M4's Search indexing (`Reader.tsx`/`PdfReader.tsx`) and M5's OCR text. The accepted design: scroll-position-ratio synchronization, matching the accepted v0.5 prototype's own reference implementation exactly, not unvalidated per-paragraph cross-format alignment. `crates/domain/src/alignment.rs` validates an Alignment Package's two fingerprints against the real Library and returns a typed `SourceNotInLibrary` mismatch rather than guessing a pairing; `AlignmentMapping::status()` flags any non-1:1 correspondence for review. `BilingualReader.tsx` (two independent scrollable text panes, swap, sync toggle, read-only Alignment panel) never calls any per-book state-mutating command, so "independent progress/notes/assets" holds by construction, not by a guard. 9 new domain tests (143 total, was 134), 6 new frontend tests (74 total, was 68), CI green.

**M6 summary (full detail in `ROADMAP.md` Milestone 6 section):** a new `crates/domain/src/calendar.rs` module adds a day-keyed Calendar layer (`DESIGN.md` SS13, canonical `ER-CAL-001`) strictly on top of the existing M3 facts -- `record_active_reading_time_command` computes the same `active_duration` figure it always did and now folds it into both the per-book ledger (unchanged) and a new day-keyed aggregate, never a second independently-derived source of "how much was read." "Planned Book Hours" and "lightweight goals" were deliberately unified into one mechanism (a single global daily reading-time-goal number, no streaks/badges/per-book due dates), since frozen `PRODUCT_SPEC.md` SS9/SS15 defines no due-date/pacing algorithm to distribute per-book Book Hours across calendar days. Explainable historical reporting is structural: `daily_goal_history` is append-only and keyed by effective day. A `Calendar.tsx` surface matches the accepted prototype's Calendar composition. 10 new domain tests (134 total, was 124), 5 new frontend tests (68 total, was 63), CI green.

**M5 summary (full checkpoint history preserved in git log and `tooling/m5-evidence/`):** the reused PP-OCRv6 pipeline (detector, orientation classifier, recognizer) was root-caused past two double-activation bugs (sigmoid, then softmax) and a panicking `imageproc::geometry` dependency, ported into real production code with 3 real ONNX sessions, verified end-to-end against real fixtures including a real, public-domain, genuinely degraded 1893 page. Wired into a real Tauri command and a dedicated `OcrWorkspace.tsx` surface (`DESIGN.md` SS10 canonical `ER-OCR-001`). **Self-correction:** M5 was first marked Complete at commit `51e506e` on Exit Gate/Success Evidence grounds alone; a follow-up self-review against `DESIGN.md`'s frozen UI-authority section found a real UI-composition gap, closed at commit `67d32c5`. Full Exit Gate evidence: `ROADMAP.md` Milestone 5 section. 124 domain tests, 63 frontend tests, 2 local-only real-fixture integration tests, CI green.

Current Blockers: None. Honest verification/measurement gaps carried into M8, none new: (1) the selection-driven Highlight/Excerpt UI (M4), M5's OCR UI, M6's Calendar/goal-editing UI, and M7's dual-pane Bilingual Reading UI have not been exercised via a live native-window click-through and screenshotted -- this sandbox's `SetForegroundWindow` residual (see `[[feedback-native-gui-visual-verification]]`) makes scripted drag-select/click-through unreliable to script, and Tauri's `invoke()` IPC bridge only exists inside the native WebView, so a plain browser preview cannot exercise any of these flows at all; (2) the uncapped OCR recognition-width change has not been re-measured for its specific accuracy impact; (3) the daily reading goal is a single global value, not per-book/per-week, a deliberate lightweight-scope choice; (4) M7's per-paragraph click-to-align highlighting and per-side Notebook toolbar are deliberately not built, per the scope decision in `tooling/m7-evidence/`. Confidence for (1) comes from source-level correctness review plus clean typecheck/build/CI. A future session with reliable interactive GUI verification, or the user's own manual click-through, should close it.
Open Escalations: None
Architecture State: **Accepted Architecture Baseline** — see `M0_ARCHITECTURE_DECISION.md`. Amendments still require reconciliation under the Architecture Promotion Rule; Product-Contract Conflicts still require a Human Gate. M0's accepted OCR backend decision (§8, closed out by M5): a PP-OCR-class ONNX pipeline via the Rust `ort` crate. M7 closed the `FORMAT_CAPABILITY_MATRIX.md` `🧪` markers on Bilingual Alignment Package via a recorded Loop Engineering evidence pass (`tooling/m7-evidence/m7a_bilingual_alignment_scope_decision.md`) rather than by silently building past them -- see that doc and `ROADMAP.md` Milestone 7 for the full Question/Evidence/Finding/ACCEPT record. M0's carried-forward OCR residuals: CJK/Latin word-boundary spacing loss -- still open; multi-column text splicing -- **closed** (M5); vertical classical-Chinese reading order -- still open, root-caused, carried forward explicitly (see `tooling/m5-evidence/m5e_multi_column_cjk_evidence.md`). Dev-environment findings, all isolated to this local sandbox and confirmed non-issues on CI's MSVC `windows-latest` runner (which remains authoritative): (1) `cargo test` on this sandbox's GNU-hosted toolchain is unstable once Tauri's COM/WebView2 dependency graph links into a test binary — worked around via the Tauri-free `crates/domain` member; (2) a real `cargo build` originally failed to *link* locally (`ld: export ordinal too large`) — resolved by dropping the unused `cdylib` crate-type; (3) `npm run tauri dev`'s cargo-watch supervision is unstable for long-lived native visual checks — launching the already-built `target/debug/ebookreader.exe` directly against a plain standalone `npm run dev` vite server is the stable method; (4) capture native screenshots via `PrintWindow(hwnd, hdc, PW_RENDERFULLCONTENT)` rather than `CopyFromScreen` (see `[[feedback-native-gui-visual-verification]]` for the full method).
Feature Complete: No
Feature Freeze: No
RC / Release State: Not started
Next Action: Begin M8 — Data Safety, Restore & Update Awareness, the final Milestone in the M1→M8 autonomous envelope. Per `ROADMAP.md`: deliver recovery snapshots, backups, real Restore, relink/recovery, Library Health, destructive-data safeguards, and V1 update awareness (App Data Backup; Full Library Backup; Restore Preview; clean-state Restore; Managed-Copy recovery; Reference relink -- already built in M1, reuse not rebuild; canonical user-asset round trip; destructive confirmation behavior; background stable-release check; manual Check Now; Up To Date / Update Available / Check Failed; draft/prerelease ignored; offline-safe behavior; no silent auto-install). Stop/Escalate if Restore cannot prove a canonical-data round trip, destructive behavior remains ambiguous, or update awareness becomes a dependency for core reading. Exit Gate: "Stable V1 feature implementation is not Feature Complete until real Restore works" -- this is the highest-risk Milestone (Risk: High) and the one whose Exit Gate gates Feature Complete itself, so the M0 corrective-pass discipline (real backup->restore->verify round trip, not a mocked one) applies at full strength. Read `PRODUCT_SPEC.md` SS16-17 and `DESIGN.md` SS14 before designing anything. After M8's Exit Gate is genuinely satisfied (not just its own Success Evidence bullets -- cross-check `DESIGN.md`'s Data/Recovery UI authority the same way M5's gap was caught), this session's `/goal` envelope (M2->M8 -> Feature Complete Candidate) reaches its own completion condition; the next Human Gate is the Human Feature Freeze Gate. No further per-Milestone human approval is required before then unless a stop/escalation trigger fires (`ROADMAP.md` §1.8).
