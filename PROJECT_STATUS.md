# EbookReader Project Status

Last Updated: 2026-09-09 (continued)

Current Phase: M5 — Scanned PDF OCR (autonomous feature loop, M1→M8 envelope)
Current Milestone: M5 — **In Progress** (M1 — **Complete**, `94aff83`; M2 — **Complete**, `0a04fb4`; M3 — **Complete**, `096b90d` + durability test; M4 — **Complete**, `7f12173`, full Exit Gate evidence in `ROADMAP.md`)
Current Checkpoint / Promotion Unit: M5 checkpoint 3 — OCR model-asset sourcing decision + real Rust `ort` spike against the reused detector, in progress (paused per user direction — see Next Action)
Current Branch / PR: `main` (direct commits; no PR yet — single long-lived checkpoint per `ROADMAP.md` §1.1/§1.6)
Last Accepted Commit: `4ab698e` (M5: OCR job/scope/status + raw-cache-vs-correction data model — CI green); this checkpoint's evidence (`tooling/m5-evidence/`) is being committed alongside this status update. Checkpoint 1 (`1a9a367`) shipped PdfReader.tsx's scanned-PDF degraded-state notice (SS13.1/SS13.2). Checkpoint 2 (`4ab698e`) added `crates/domain/src/ocr.rs`: `OcrScope`, `OcrJobStatus` with explicit terminal states, and — directly proving the Exit Gate condition — a canonical `ocr_correction` table structurally separate from the rebuildable `ocr_page_result` cache, with a domain test proving a correction survives a cache clear. 91 domain tests, 7 OCR Tauri commands, 63 frontend tests unaffected. **This checkpoint (model sourcing):** asked the user rather than silently downloading a model (per this session's own safety rules on file downloads). The user directed reuse of already-license-cleared PP-OCRv6 assets already present on this machine from prior, unrelated local OCR work. Copied `PP-OCRv6_det_medium.onnx` (62,119,454 bytes), `PP-OCRv6_rec_small.onnx` (21,234,383 bytes), `ch_ppocr_mobile_v2.0_cls_mobile.onnx` (585,532 bytes), and `onnxruntime.dll`/`onnxruntime_providers_shared.dll` into this repo's already-gitignored `ocr-assets/` (confirmed via `git check-ignore` and `git status` that nothing binary is tracked). Hashes, license provenance (a prior, already-passed Redistribution & Licensing Compliance Audit on this exact asset set, Apache-2.0 via PaddlePaddle/RapidOCR ModelScope), and documented DBNet pre/post-processing parameters recorded in `tooling/m5-evidence/README.md`. Then ran a real Rust `ort` spike (extending M0-F's) against the reused detector: found and fixed two wrong assumptions inherited from the M0-F model (output tensor name, and the `limit_side_len` resize condition's direction — corrected against a real, already-validated reference implementation, not guessed) — the model now loads and runs to completion from Rust on two real fixtures, but the textness output is **not yet confirmed correct** (suspiciously flat ~0.5-0.73 band on both a real scan and a clean synthetic fixture; leading hypothesis is an RGB/BGR channel-order mismatch, not yet tested). Full findings: `tooling/m5-evidence/m5a_det_medium_rust_ort_spike.md`.
Current Blockers: None. One honest verification gap, not a blocker, carried from M4: the selection-driven Highlight/Excerpt UI has not been exercised via a real mouse drag-select in a live native window and screenshotted — this sandbox's `SetForegroundWindow` residual (see `[[feedback-native-gui-visual-verification]]`) makes scripted drag-select-then-screenshot unreliable, judged not worth burning further turns retrying. Confidence instead comes from direct source-level reading of foliate-js's/pdfjs-dist's actual APIs plus clean typecheck/build/CI. A future session with reliable interactive GUI verification, or the user's own manual click-through, should close this gap.
Open Escalations: None
Architecture State: **Accepted Architecture Baseline** — see `M0_ARCHITECTURE_DECISION.md`. Amendments still require reconciliation under the Architecture Promotion Rule; Product-Contract Conflicts still require a Human Gate. M0's accepted OCR backend decision (§8): a PP-OCR-class ONNX pipeline via the Rust `ort` crate (dynamically-loaded `onnxruntime.dll`, MSVC target for release packaging — `ort-sys` ships no prebuilt `x86_64-pc-windows-gnu` binary). M0's carried-forward OCR residuals for M5 to own: CJK/Latin word-boundary spacing loss in OCR output; multi-column text splicing across column boundaries; incorrect reading order on vertical classical-Chinese layouts (all confirmed on real pages during M0's corrective pass, not hypothetical) — M5's correction-mapping and layout-analysis design must plan for these. Dev-environment findings, all isolated to this local sandbox and confirmed non-issues on CI's MSVC `windows-latest` runner (which remains authoritative): (1) `cargo test` on this sandbox's GNU-hosted toolchain is unstable once Tauri's COM/WebView2 dependency graph links into a test binary — worked around via the Tauri-free `crates/domain` member; (2) a real `cargo build` originally failed to *link* locally (`ld: export ordinal too large`) — resolved by dropping the unused `cdylib` crate-type; (3) `npm run tauri dev`'s cargo-watch supervision is unstable for long-lived native visual checks — launching the already-built `target/debug/ebookreader.exe` directly against a plain standalone `npm run dev` vite server is the stable method; (4) capture native screenshots via `PrintWindow(hwnd, hdc, PW_RENDERFULLCONTENT)` rather than `CopyFromScreen` — handle-based, cannot mis-capture an unrelated window, and needs no foreground at all (see `[[feedback-native-gui-visual-verification]]` for the full method).
Feature Complete: No
Feature Freeze: No
RC / Release State: Not started
Next Action: **per explicit user instruction this session, further Milestone work is paused here** ("Hold at M5" -- do not start M6/M7/M8 in parallel) while the OCR model-sourcing question was resolved; that question is now resolved (reuse of already-locally-available license-cleared assets, done, see above), but a live decision from the user is still needed on where the M5-A spike's investigation goes next -- this is a natural pause point for the user to review the evidence (`tooling/m5-evidence/`) before more turns are spent on it. The concrete next technical step, if directed to continue: resolve the M5-A spike's RGB/BGR channel-order hypothesis (compare Rust `image`-crate-decoded RGB bytes against the reference implementation's actual input channel order at its call site, not just its preprocess function read so far), then validate detection output correctness (visual/numeric comparison against a known-good detector output on the same fixture) before treating detection as ACCEPTed. Postprocessing (contour extraction + polygon unclipping -- currently OpenCV/`pyclipper` in the reference implementation) has no Rust port and needs its own architecture decision (an OpenCV-equivalent Rust crate vs. hand-rolled geometry) before it can start. Recognition (`PP-OCRv6_rec_small.onnx`) and the orientation classifier haven't been touched yet. The OCR job/correction data model (`crates/domain/src/ocr.rs`) and the pre-OCR degraded state (`PdfReader.tsx`) from checkpoints 1-2 are already real, tested, and committed regardless of how the detector investigation proceeds. No further per-Milestone human approval is required inside the M1→M8 envelope once work resumes unless a stop/escalation trigger fires (`ROADMAP.md` §1.8); the next Human Gate is the Human Feature Freeze Gate after M8. This session runs under an active `/goal` Stop hook (EbookReader -> Feature Complete Candidate via M2->M8) plus a `CronList`-verified watchdog job (id `4f18a51c`, `12,42 * * * *`, session-only, fires only if the goal-driven work appears stalled); if a resumed session finds neither active but this authorization envelope is still valid, resume from this file directly and re-arm the watchdog -- but should re-confirm with the user whether "Hold at M5" is still in effect before resuming autonomous Milestone-jumping, since that instruction came from the user directly in this session, not from ROADMAP.md itself.
