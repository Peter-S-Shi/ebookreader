# EbookReader Project Status

Last Updated: 2026-09-09

Current Phase: M6 — Calendar, Goals & Library Planning (autonomous feature loop, M1→M8 envelope)
Current Milestone: M6 — **Starting** (M1 — **Complete**, `94aff83`; M2 — **Complete**, `0a04fb4`; M3 — **Complete**, `096b90d` + durability test; M4 — **Complete**, `7f12173`; M5 — **Complete**, `51e506e`, full Exit Gate evidence in `ROADMAP.md`)
Current Checkpoint / Promotion Unit: M6 not yet started -- this is the transition point immediately after M5's promotion.
Current Branch / PR: `main` (direct commits; no PR yet — single long-lived checkpoint per `ROADMAP.md` §1.1/§1.6)
Last Accepted Commit: `51e506e`.

**M5 summary (full checkpoint history preserved in git log and `tooling/m5-evidence/`):** the reused PP-OCRv6 pipeline (detector, orientation classifier, recognizer) was root-caused past two double-activation bugs (sigmoid, then softmax) and a panicking `imageproc::geometry` dependency, ported into real production code (`crates/domain/src/ocr_{detect,recognize,classify,preprocess,engine}.rs`) with 3 real ONNX sessions composed in `OcrEngine::process_page()`, verified end-to-end against real fixtures including a real, public-domain, genuinely degraded 1893 page. Wired into a real Tauri command (`run_ocr_job_command`) and a full `PdfReader.tsx` UI: Current Page / Selected Pages / Entire Book scope selection, real pause/resume/cancel, a correction editor, Excerpt/Annotation capture on OCR'd text, and Library-wide Search indexing of OCR'd/corrected text. A column-aware reading-order fix (validated against the real fixture) closed multi-column English splicing; vertical-CJK reading order was root-caused to a detection-orientation problem (the detector finds cross-column horizontal bands, not per-column boxes) and explicitly carried forward as a residual needing a real architecture decision (rotation-reprocessing or a dedicated text-direction model), not a quick fix. The Exit Gate ("manual corrections survive raw OCR/cache rebuild and restart") is proven by two real automated tests, including one that closed a real gap in its own proof: every prior OCR test used an in-memory database (incapable of demonstrating restart survival at all), so a new test opens a real file-backed database, saves a correction, drops the connection, reopens a fresh one to the same file, and confirms the correction survived. Full Exit Gate evidence and residuals list: `ROADMAP.md` Milestone 5 section. 124 domain tests (was 82 at M4's close), 63 frontend tests, 2 local-only real-fixture integration tests, CI green on every M5 commit.

Current Blockers: None. Two honest verification gaps carried into M6, neither new: (1) the selection-driven Highlight/Excerpt UI (M4) and M5's OCR trigger/scope/pause/correction UI have not been exercised via a live native-window click-through and screenshotted -- this sandbox's `SetForegroundWindow` residual (see `[[feedback-native-gui-visual-verification]]`) makes scripted drag-select/click-through unreliable to script, and Tauri's `invoke()` IPC bridge only exists inside the native WebView, so a plain browser preview cannot exercise either flow at all; (2) the uncapped OCR recognition-width change has not been re-measured for its specific accuracy impact. Confidence for (1) comes from source-level correctness review plus clean typecheck/build/CI. A future session with reliable interactive GUI verification, or the user's own manual click-through, should close it.
Open Escalations: None
Architecture State: **Accepted Architecture Baseline** — see `M0_ARCHITECTURE_DECISION.md`. Amendments still require reconciliation under the Architecture Promotion Rule; Product-Contract Conflicts still require a Human Gate. M0's accepted OCR backend decision (§8, closed out by M5): a PP-OCR-class ONNX pipeline via the Rust `ort` crate (dynamically-loaded `onnxruntime.dll`, MSVC target for release packaging — `ort-sys` ships no prebuilt `x86_64-pc-windows-gnu` binary). M0's carried-forward OCR residuals, M5's disposition: CJK/Latin word-boundary spacing loss -- still open, not addressed this Milestone; multi-column text splicing -- **closed**, column-aware reading order shipped and validated against a real fixture; vertical classical-Chinese reading order -- still open, root-caused to a detection-orientation problem requiring a real architecture decision, carried forward explicitly (see `tooling/m5-evidence/m5e_multi_column_cjk_evidence.md`). Dev-environment findings, all isolated to this local sandbox and confirmed non-issues on CI's MSVC `windows-latest` runner (which remains authoritative): (1) `cargo test` on this sandbox's GNU-hosted toolchain is unstable once Tauri's COM/WebView2 dependency graph links into a test binary — worked around via the Tauri-free `crates/domain` member; (2) a real `cargo build` originally failed to *link* locally (`ld: export ordinal too large`) — resolved by dropping the unused `cdylib` crate-type; (3) `npm run tauri dev`'s cargo-watch supervision is unstable for long-lived native visual checks — launching the already-built `target/debug/ebookreader.exe` directly against a plain standalone `npm run dev` vite server is the stable method; (4) capture native screenshots via `PrintWindow(hwnd, hdc, PW_RENDERFULLCONTENT)` rather than `CopyFromScreen` — handle-based, cannot mis-capture an unrelated window, and needs no foreground at all (see `[[feedback-native-gui-visual-verification]]` for the full method).
Feature Complete: No
Feature Freeze: No
RC / Release State: Not started
Next Action: Begin M6 — Calendar, Goals & Library Planning. Per `ROADMAP.md`: deliver Planned Book Hours, an Actual Reading Time calendar, lightweight goals, and explainable historical reporting, without growing into gamification (Stop/Escalate if feature growth becomes required to make the workflow coherent, or if factual historical time would be retroactively altered). M3 already built the underlying data (`crates/domain/src/book_hours.rs`, `actual_reading_time.rs`) -- M6's job is the Calendar/Goals layer on top of that real data, not rebuilding it. Exit Gate: "Calendar stays lightweight and semantically distinct from factual reading-time history." First step: read `PRODUCT_SPEC.md`'s Calendar/Goals section (and `ARCHITECTURE.md`/`DESIGN.md` for any already-frozen UI authority on this) before designing anything, per this project's own Inspect-before-Define discipline. No further per-Milestone human approval is required inside the M1→M8 envelope unless a stop/escalation trigger fires (`ROADMAP.md` §1.8); the next Human Gate is the Human Feature Freeze Gate after M8. The `/goal` Stop hook (EbookReader -> Feature Complete Candidate via M2->M8) remains active for this session.
