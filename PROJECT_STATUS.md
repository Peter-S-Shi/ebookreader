# EbookReader Project Status

Last Updated: 2026-09-09 (continued)

Current Phase: M5 — Scanned PDF OCR (autonomous feature loop, M1→M8 envelope)
Current Milestone: M5 — **In Progress** (M1 — **Complete**, `94aff83`; M2 — **Complete**, `0a04fb4`; M3 — **Complete**, `096b90d` + durability test; M4 — **Complete**, `7f12173`, full Exit Gate evidence in `ROADMAP.md`)
Current Checkpoint / Promotion Unit: M5 checkpoint 2 — OCR job/scope/status + raw-cache-vs-correction data model, in progress
Current Branch / PR: `main` (direct commits; no PR yet — single long-lived checkpoint per `ROADMAP.md` §1.1/§1.6)
Last Accepted Commit: `4ab698e` (M5: OCR job/scope/status + raw-cache-vs-correction data model — CI green). Checkpoint 1 (`1a9a367`) shipped PdfReader.tsx's scanned-PDF degraded-state notice (SS13.1/SS13.2). This checkpoint adds `crates/domain/src/ocr.rs`: `OcrScope` (CurrentPage/SelectedPages/EntireBook), `OcrJobStatus` with explicit terminal Succeeded/Failed variants, and — directly proving the Exit Gate condition — a canonical `ocr_correction` table kept structurally separate from the rebuildable `ocr_page_result` cache (`clear_page_results` only ever touches the cache; a domain test proves a correction survives a cache clear). `effective_text()` prefers a correction, else the raw result, else `None` — never a placeholder, so nothing can pretend OCR text exists before OCR has run (SS13.2). 9 new domain tests (91 total); 7 new Tauri commands. M4's full evidence account lives in `ROADMAP.md`'s M4 Exit Gate section, not duplicated here (same policy as M1-M3). 63 frontend tests unaffected, CI green on every commit.
Current Blockers: None. One honest verification gap, not a blocker, carried from M4: the selection-driven Highlight/Excerpt UI has not been exercised via a real mouse drag-select in a live native window and screenshotted — this sandbox's `SetForegroundWindow` residual (see `[[feedback-native-gui-visual-verification]]`) makes scripted drag-select-then-screenshot unreliable, judged not worth burning further turns retrying. Confidence instead comes from direct source-level reading of foliate-js's/pdfjs-dist's actual APIs plus clean typecheck/build/CI. A future session with reliable interactive GUI verification, or the user's own manual click-through, should close this gap.
Open Escalations: None
Architecture State: **Accepted Architecture Baseline** — see `M0_ARCHITECTURE_DECISION.md`. Amendments still require reconciliation under the Architecture Promotion Rule; Product-Contract Conflicts still require a Human Gate. M0's accepted OCR backend decision (§8): a PP-OCR-class ONNX pipeline via the Rust `ort` crate (dynamically-loaded `onnxruntime.dll`, MSVC target for release packaging — `ort-sys` ships no prebuilt `x86_64-pc-windows-gnu` binary). M0's carried-forward OCR residuals for M5 to own: CJK/Latin word-boundary spacing loss in OCR output; multi-column text splicing across column boundaries; incorrect reading order on vertical classical-Chinese layouts (all confirmed on real pages during M0's corrective pass, not hypothetical) — M5's correction-mapping and layout-analysis design must plan for these. Dev-environment findings, all isolated to this local sandbox and confirmed non-issues on CI's MSVC `windows-latest` runner (which remains authoritative): (1) `cargo test` on this sandbox's GNU-hosted toolchain is unstable once Tauri's COM/WebView2 dependency graph links into a test binary — worked around via the Tauri-free `crates/domain` member; (2) a real `cargo build` originally failed to *link* locally (`ld: export ordinal too large`) — resolved by dropping the unused `cdylib` crate-type; (3) `npm run tauri dev`'s cargo-watch supervision is unstable for long-lived native visual checks — launching the already-built `target/debug/ebookreader.exe` directly against a plain standalone `npm run dev` vite server is the stable method; (4) capture native screenshots via `PrintWindow(hwnd, hdc, PW_RENDERFULLCONTENT)` rather than `CopyFromScreen` — handle-based, cannot mis-capture an unrelated window, and needs no foreground at all (see `[[feedback-native-gui-visual-verification]]` for the full method).
Feature Complete: No
Feature Freeze: No
RC / Release State: Not started
Next Action: two M5 checkpoints are done — the pre-OCR degraded state, and the OCR job/scope/status/correction data model (job scope selection, pause/resume/cancel status transitions, an explicit success/fail terminal state, and correction durability across a cache rebuild are all real and tested at the domain layer now). **The remaining, load-bearing blocker for the rest of M5 is sourcing an actual OCR engine**: M0's accepted backend (SS8) is a PP-OCR-class ONNX pipeline via the Rust `ort` crate, and a filesystem search this session confirmed no `.onnx` model file or `onnxruntime*.dll` exists anywhere in this repo or sandbox — the M0-F spike that proved this approach ran against a model/runtime that isn't present now. Getting a real model means either downloading a redistributable PP-OCR ONNX model (PaddleOCR ships these under Apache-2.0) plus a dynamically-loadable `onnxruntime.dll`, or another sourcing path with equivalent evidence. **Downloading any file requires explicit user permission per this session's own safety rules** — this is not a routine Milestone-boundary decision the `/goal` envelope can wave through, so the next session/turn should raise it to the user directly rather than silently downloading a multi-ten-MB binary into a public repo. Once a real model exists, the actual OCR engine (detection -> box extraction -> recognition -> decode, wired to `ocr::save_page_result`) is the next real checkpoint, followed by the frontend job-trigger/progress/correction UI, and finally closing the real degraded cases M0 already confirmed (multi-column splicing, vertical CJK reading order, word-boundary spacing loss). This is real, high-risk engineering (a new native dependency, a background job system, a correction UI) — expect several more checkpoints, each with its own spike-then-evidence account, not one. No further per-Milestone human approval is required inside the M1→M8 envelope unless a stop/escalation trigger fires (`ROADMAP.md` §1.8); the next Human Gate is the Human Feature Freeze Gate after M8. This session runs under an active `/goal` Stop hook (EbookReader -> Feature Complete Candidate via M2->M8) plus a `CronList`-verified watchdog job (id `4f18a51c`, `12,42 * * * *`, session-only, fires only if the goal-driven work appears stalled); if a resumed session finds neither active but this authorization envelope is still valid, resume from this file directly and re-arm the watchdog.
