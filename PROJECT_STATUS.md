# EbookReader Project Status

Last Updated: 2026-09-09

Current Phase: M5 — Scanned PDF OCR (autonomous feature loop, M1→M8 envelope)
Current Milestone: M5 — **In Progress** (M1 — **Complete**, `94aff83`; M2 — **Complete**, `0a04fb4`; M3 — **Complete**, `096b90d` + durability test; M4 — **Complete**, `7f12173`, full Exit Gate evidence in `ROADMAP.md`)
Current Checkpoint / Promotion Unit: M5 checkpoint 5 — the full OCR pipeline (detect, classify orientation, recognize, CTC-decode, reading-order assembly) is now real production code wired end-to-end from a Tauri command to a frontend "Run OCR" button, verified against a real fixture.
Current Branch / PR: `main` (direct commits; no PR yet — single long-lived checkpoint per `ROADMAP.md` §1.1/§1.6)
Last Accepted Commit: `16eed51`. History: checkpoint 1 (`1a9a367`) shipped the scanned-PDF degraded-state notice (SS13.1/13.2). Checkpoint 2 (`4ab698e`) added the OCR job/correction data model (`crates/domain/src/ocr.rs`) with a domain test directly proving the Exit Gate condition (a correction survives a raw-cache clear). Checkpoint 3 resolved model sourcing per explicit user direction (reuse of already-license-cleared PP-OCRv6 assets already present on this machine from prior, unrelated local OCR work; hashes/provenance in `tooling/m5-evidence/README.md`). Checkpoint 4 (`55130a6`) root-caused the detector's degenerate output to a double-sigmoid bug (not channel order, empirically ruled out) and shipped a working postprocessing pipeline (`ocr_detect.rs`) after finding and replacing two panicking `imageproc::geometry` calls with a hand-rolled hull/rect implementation — 142/142 real boxes recovered on the fixture, matching the Python reference exactly.

**This checkpoint:** ported the validated detection+postprocessing spike into production, then closed out the two remaining reused-model stages and wired everything together. Recognition (`PP-OCRv6_rec_small.onnx`): its embedded ONNX metadata carries the model's own 18,708-character dictionary (no separate download needed), and its output is also already softmax-activated -- the same pattern as the detector, caught the same way (an implausible uniform confidence value, not a crash). Ported to `crates/domain/src/ocr_recognize.rs` (CTC greedy decode, 5 unit tests). Orientation classifier (`ch_ppocr_mobile_v2.0_cls_mobile.onnx`, untouched until now): also already-softmax, wired via `ocr_classify.rs` (4 unit tests). All three models are now composed in `crates/domain/src/ocr_engine.rs`'s `OcrEngine::process_page()`, plus `ocr_preprocess.rs` (10 unit tests pinned to real values from the spikes) and `reading_order_text()` (row-bucket-then-left-to-right assembly, 4 unit tests -- explicitly does not solve multi-column or vertical-CJK order, both remain M0-carried residuals). A local-only (`#[ignore]`d, no CI model assets) integration test runs the full 3-model pipeline against the real fixture: passes, ~103s wall time. 121 domain tests total (was 91 before this session), all passing, plus the 1 ignored integration test.

Then wired this into the app: `run_ocr_job_command` (Tauri) runs the pipeline over PNG-encoded page bytes and saves results via `ocr::save_page_result`, lazily loading the engine once via new `OcrEngineState`. `PdfReader.tsx`'s scanned-PDF panel now has a real "Run OCR on this page" button (`canvas.toDataURL` -> bytes -> the command), displays the recognized text, offers a "Correct text" editor wired to `save_ocr_correction_command`, and loads any already-saved effective text on page load. Scoped to Current Page only; Selected Pages/Entire Book scope selection in this UI is a follow-up. 63 frontend tests unaffected; full `ebookreader` Tauri build clean. Full findings: `tooling/m5-evidence/m5c_recognition_spike.md`, `m5d_ocr_engine_production_port.md`.
Current Blockers: None. Two honest verification gaps, not blockers: (1) carried from M4, the selection-driven Highlight/Excerpt UI has not been exercised via a real mouse drag-select in a live native window and screenshotted -- this sandbox's `SetForegroundWindow` residual (see `[[feedback-native-gui-visual-verification]]`) makes scripted drag-select-then-screenshot unreliable; (2) new this checkpoint, the "Run OCR" button/correction-editor flow has not been click-tested in a live native window either, for the same reason -- Tauri's `invoke()` IPC bridge only exists inside the native WebView, so a plain browser preview cannot exercise it at all, not even partially. Confidence for both comes from source-level correctness review plus clean typecheck/build/CI (63 frontend + 121 domain tests). A future session with reliable interactive GUI verification, or the user's own manual click-through, should close these gaps.
Open Escalations: None
Architecture State: **Accepted Architecture Baseline** — see `M0_ARCHITECTURE_DECISION.md`. Amendments still require reconciliation under the Architecture Promotion Rule; Product-Contract Conflicts still require a Human Gate. M0's accepted OCR backend decision (§8): a PP-OCR-class ONNX pipeline via the Rust `ort` crate (dynamically-loaded `onnxruntime.dll`, MSVC target for release packaging — `ort-sys` ships no prebuilt `x86_64-pc-windows-gnu` binary). M0's carried-forward OCR residuals for M5 to own: CJK/Latin word-boundary spacing loss in OCR output; multi-column text splicing across column boundaries; incorrect reading order on vertical classical-Chinese layouts (all confirmed on real pages during M0's corrective pass, not hypothetical) — M5's correction-mapping and layout-analysis design must plan for these. Dev-environment findings, all isolated to this local sandbox and confirmed non-issues on CI's MSVC `windows-latest` runner (which remains authoritative): (1) `cargo test` on this sandbox's GNU-hosted toolchain is unstable once Tauri's COM/WebView2 dependency graph links into a test binary — worked around via the Tauri-free `crates/domain` member; (2) a real `cargo build` originally failed to *link* locally (`ld: export ordinal too large`) — resolved by dropping the unused `cdylib` crate-type; (3) `npm run tauri dev`'s cargo-watch supervision is unstable for long-lived native visual checks — launching the already-built `target/debug/ebookreader.exe` directly against a plain standalone `npm run dev` vite server is the stable method; (4) capture native screenshots via `PrintWindow(hwnd, hdc, PW_RENDERFULLCONTENT)` rather than `CopyFromScreen` — handle-based, cannot mis-capture an unrelated window, and needs no foreground at all (see `[[feedback-native-gui-visual-verification]]` for the full method).
Feature Complete: No
Feature Freeze: No
RC / Release State: Not started
Next Action: Continue M5 autonomously (no further per-checkpoint approval required unless a genuine ROADMAP Stop/Escalation condition fires). The full pipeline now runs end-to-end for real, from a real UI trigger, on one real fixture; remaining M5 Success Evidence items, in order: (1) exercise the pipeline against additional real fixtures (multi-column layout, CJK text, a more degraded/noisy scan) to widen coverage beyond the single fixture used so far, and use those runs to actually measure whether the uncapped recognition width (`m5d_ocr_engine_production_port.md`'s residual) improves accuracy; (2) extend scope selection in the frontend beyond Current Page to Selected Pages and Entire Book (`run_ocr_job_command` already accepts a `pages` list, so this is UI-side); (3) wire real mid-run pause/cancel and incremental progress reporting (currently synchronous, no progress callback); (4) close M0's carried-forward OCR degraded cases with real evidence: multi-column splicing and vertical-CJK reading order are explicitly unsolved by the current `reading_order_text` heuristic (single-column top-to-bottom/left-to-right only) -- this needs either a real layout-analysis pass or an honestly-scoped decision to defer it further with justification, not silent neglect; word-boundary spacing loss also remains open. M5 is not Complete and M6 must not start until the Exit Gate ("manual corrections survive raw OCR/cache rebuild and restart") is evidenced end-to-end -- the data-model half has been proven since checkpoint 2, and this checkpoint proved the pipeline produces and saves real text through the same real `save_page_result`/`save_correction` path, but a full click-through restart-survival demonstration is still owed. The `/goal` Stop hook (EbookReader -> Feature Complete Candidate via M2->M8) remains active for this session.
